{
  "name": "review",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preinstall": "mkdir dist",
    "watch": "mkdir -p dist && watchify src/main.coffee -t coffeeify --extension=\".coffee\" -o dist/main.dist.js -d -v"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "coffeeify": "^1.1.0",
    "watchify": "^3.2.2"
  }
}
